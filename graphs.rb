# frozen_string_literal: true

class MoveGraph
  def initialize(move_array)
    build_graph(move_array)
  end

  def build_graph(array)
    # Make a graph of all current mossible moves
  end
  end

# Make a Tree, or graph, that can display all of the knight's current moves?
# Make an adjascency list for how knight can get from one place to another, then pick the shortest list?
